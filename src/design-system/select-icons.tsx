import { Check } from "lucide-react";

/** Icon mục đang chọn trong dropdown Select (Ant Design) — dùng chung admin + shop. */
export const SELECT_MENU_CHECK = <Check size={14} strokeWidth={2.5} aria-hidden />;
