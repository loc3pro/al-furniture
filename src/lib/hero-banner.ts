/**
 * Khớp khung hiển thị carousel trang chủ (`HeroCarousel`: `.slideLink` 19:9).
 * Dùng khi crop ảnh banner admin để upload đúng tỉ lệ, tránh méo / cắt lệch trên storefront.
 */
export const HERO_BANNER_ASPECT = 19 / 9;

/** Chiều ngang ảnh xuất sau crop (đủ cho màn Retina / fullscreen). */
export const HERO_BANNER_OUTPUT_WIDTH = 1920;
