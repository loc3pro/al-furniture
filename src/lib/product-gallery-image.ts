/** Tỉ lệ ảnh gallery biến thể sản phẩm — khung dọc 3:4 (crop trước upload). */
export const PRODUCT_GALLERY_ASPECT = 3 / 4;

/** Chiều ngang JPEG sau crop (đủ hiển thị retina). */
export const PRODUCT_GALLERY_OUTPUT_WIDTH = 1600;
