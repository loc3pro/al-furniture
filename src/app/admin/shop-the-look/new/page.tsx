import { AdminShopTheLookEditor } from "@/components/admin/AdminShopTheLookEditor";

export const metadata = { title: "Tạo Shop the Look — Admin" };

export default function AdminShopTheLookNewPage() {
  return <AdminShopTheLookEditor mode="create" />;
}
